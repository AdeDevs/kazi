# Base URL of the KaziHub backend API (including the /api/v1 prefix).
# Defaults to the hosted backend if unset -- override for local backend development.
VITE_API_BASE_URL=https://kazihub-52ph.onrender.com/api/v1
